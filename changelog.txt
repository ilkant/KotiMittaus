KotiMittaus ohjelmien Changelog.

## [0.8.5] - 2026-06-10
- settings.ini — uusi asetus log_level (oletus koodissa 0, ini:ssä nyt 1):
  - 0 = ei kirjoiteta mitään
  - 1 = "Tiedot saatu laitteelta X (laitteen nimi): N lukemaa"
  - 2 = yksityiskohtaisempi suoritus: pollauksen alku/asetukset, kierrokset, lukuyritykset, virheet, keskeytys/aikaraja, Excel-tallennus
  - 4 = kaikki näytölle tulostettava teksti peilataan lokiin (3 toimii kuten 2)

## [0.8.4] - 2026-06-10
 settings.ini — uudet asetukset [settings]-osioon:
  - polling_interval = 60 — pollauskierrosten väli sekunteina
  - polling_time = 30 — pollauksen enimmäiskesto minuutteina
  - quit_shortcut = q — keskeytysnäppäin

  health.py:
  - load_config() (health.py:920) lukee uudet asetukset; virheellinen luku → oletukset, quit_shortcut-arvosta käytetään ensimmäinen merkki
  - start_quit_listener() (health.py:776) — asettaa terminaalin cbreak-tilaan ja kuuntelee näppäimistöä asyncio-loopissa; palauttaa terminaalin ennalleen lopuksi. Putkitetussa ajossa (ei tty) kuuntelija ohitetaan.
  - collect_devices() (health.py:809) — sai quit_key-parametrin; q-painallus keskeyttää odotuksen heti (asyncio.wait_for(quit_event.wait(), ...)) ja lopettaa kierrokset. Huomaa: kesken aktiivisen BLE-luvun painallus huomioidaan vasta luvun
  päätyttyä.
  - Pääohjelma välittää asetukset collect_devices-kutsuun (health.py:1052)

## [0.8.3] - 2026-06-10
- Äänimerkki kun tiedot laitteelta on saatu tietokoneelle.
- beep() (health.py:756-770) — soittaa freedesktop-teeman "complete"-merkkiäänen paplay-komennolla. Ääni menee PulseAudio/PipeWiren oletuslaitteeseen: kun kuulokkeesi ovat aamulla pois päältä, järjestelmä ohjaa äänen automaattisesti koneen
  kaiuttimiin. Jos toisto epäonnistuu, varalla on terminaalin kellomerkki, eikä virhe koskaan keskeytä mittausten lukua.
- Kutsu lisätty collect_devices-silmukkaan kohtaan, jossa laitteelta on saatu tiedot (health.py:835) — eli piippaus kuuluu erikseen jokaisesta laitteesta heti kun sen siirto onnistui. Näin tiedät, milloin voit lopettaa lähetysnapin painelun
  kullakin laitteella.

## [0.8.2] - 2026-06-09
- Use own measurement class for every device
- Read devices multiple measurements as a list

  measurement.py
  - combine_measurements_by_day(device_results) — one Measurement per calendar day; if a device has several readings on the same day, the newest (by timestamp) wins. Readings without a timestamp are skipped (can't be placed by date).
  - write_measurements_by_date(filename, measurements) — builds a date→row index from column 1, finds each measurement's day, and fills only its non-empty metric columns (cols 2…N), leaving the pre-filled date and untouched metrics alone. Returns
  (written, unmatched); days not found in the sheet (wrong year/month) come back as unmatched.
  - Helpers _cell_to_date (handles datetime, date, and string forms like YYYY-MM-DD / DD.MM.YYYY) and find_measurement_sheet (locates the sheet by its "Päiväys…" header, so it works on both the Mittaukset sheet and the externally-created
  terveysdata_* sheets).
  - Fixed a pre-existing syntax error: line 24 was a corrupted cla instead of class DeviceMeasurement: — the module didn't import at all before this.

  health.py
  - create_new_file(filename, period, ref_date) now pre-populates one row per day — the whole year (1.1.–31.12.) or a single month, per period.
  - load_config returns period ("month" if the filename template uses {month}, else "year").
  - Main flow combines readings by day, creates the day-row file if missing, writes by date, and reports written days plus any unmatched ones.

  Verified: 20-reading Contour list spread across days lands on correct rows; same-day duplicate BP keeps the newest; a glucose-only day doesn't wipe another day's weight; wrong-year readings reported as unmatched; works on both freshly-created
  (datetime) and pre-built (string-date) sheets; year/month file creation produce correct day counts.

  One behavior worth confirming: since the target sheet has one row per day, only one value per metric per day is kept (newest). If you'd ever want multiple same-day readings preserved, that would need a different sheet layout.

## [0.8.1] - 2026-06-06
### Added
- Test program can produce multiple measurements. Like Contour sends about 10 measurements
- Test program can store individual device data to spreadsheet

## [0.8.0] - 2026-06-02
### Added
- Changed code to pure english
- Health can add measurement to same row if there are not those values and new row if thre are values

## [0.8.0] - 2026-06-02
### Initial
- Measurement object to values
- Health produces new output file with header row if there are not suitable file. For example new file every year.
- Can read values from Contour Care and Omron M7
